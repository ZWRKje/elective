#include <iostream>

#include "TSunSet.h"

int main() {
    std::cout << TSunSet::createInstance()->getCount() << std::endl;
    std::cout << TSunSet::createInstance()->getCount() << std::endl;
    std::cout << TSunSet::createInstance()->getCount() << std::endl;
    std::cout << TSunSet::createInstance()->getCount() << std::endl;
    return 0;
}